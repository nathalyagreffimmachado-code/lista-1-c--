#include <iostream>

using namespace std;

int main() {
    // Declaração das variáveis para armazenar os dois números inteiros
    int num1, num2, soma;

    // Leitura dos dois valores inteiros
    cin >> num1 >> num2;

    // Cálculo da soma
    soma = num1 + num2;

    // Impressão do resultado no formato solicitado
    cout << "Soma = " << soma << endl;

    return 0;
}