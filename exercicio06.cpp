#include <iostream>

using namespace std;

int main() {
    // Laço 'for' começando em 0 e parando antes de chegar a 10 (ou seja, no 9)
    for (int i = 0; i < 10; i++) {
        // Imprime o valor atual de 'i' e quebra a linha
        cout << i << endl;
    }

    return 0;
}